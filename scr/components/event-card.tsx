import Link from "next/link";
import { Badge } from "./ui";
import type { EventRow } from "@/lib/supabase-rest";
import { toDate, toTimeRange } from "@/lib/supabase-rest";
export function EventCard({ event }: { event: EventRow }) { const percent = Math.round((event.registered_count / Math.max(event.capacity,1)) * 100); return <article className="group overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><div className="h-48 bg-cover bg-center" style={{ backgroundImage: `url(${event.image_url || "https://images.unsplash.com/photo-1517048676732-d65bc937f952?auto=format&fit=crop&w=1200&q=80"})` }} /><div className="space-y-4 p-6"><div className="flex flex-wrap gap-2"><Badge>{event.category || "Sự kiện"}</Badge><Badge tone={event.status === "ended" ? "slate" : "green"}>{event.status === "ended" ? "Đã kết thúc" : "Sắp diễn ra"}</Badge></div><div><h2 className="text-xl font-bold text-slate-950 group-hover:text-blue-700">{event.title}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-slate-600">{event.description}</p></div><div className="grid gap-2 text-sm text-slate-600"><p>📅 {toDate(event.start_time)} · {toTimeRange(event.start_time,event.end_time)}</p><p>📍 {event.location}</p><p>🏫 {event.faculty || "Toàn trường"}</p></div><div><div className="mb-2 flex justify-between text-xs font-semibold text-slate-500"><span>Đăng ký</span><span>{event.registered_count}/{event.capacity}</span></div><div className="h-2 rounded-full bg-slate-100"><div className="h-2 rounded-full bg-blue-700" style={{ width: `${Math.min(percent,100)}%` }} /></div></div><Link href={`/events/${event.slug}`} className="inline-flex w-full justify-center rounded-2xl bg-slate-950 px-4 py-3 text-sm font-bold text-white">Xem chi tiết</Link></div></article>; }

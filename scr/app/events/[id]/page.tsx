import Link from "next/link";import { notFound } from "next/navigation";import { Badge, Field, PageShell, SelectField, StatCard } from "@/components/ui";import { getEvent, getFaculties, registerStudentAction, toDate, toTimeRange } from "@/lib/supabase-rest";export default async function EventDetailPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const [event,faculties]=await Promise.all([getEvent(id),getFaculties()]);if(!event)notFound();return <PageShell><div className="grid gap-8 lg:grid-cols-[1fr_420px]"><article className="overflow-hidden rounded-[2rem] border border-slate-200 bg-white shadow-sm"><div className="h-80 bg-cover bg-center" style={{backgroundImage:`url(${event.image_url||"https://images.unsplash.com/photo-1517048676732-d65bc937f952?auto=format&fit=crop&w=1200&q=80"})`}}/><div className="space-y-6 p-8"><div className="flex gap-2"><Badge>{event.category}</Badge><Badge tone="green">{event.faculty}</Badge></div><h1 className="text-4xl font-black text-slate-950">{event.title}</h1><p className="text-lg leading-8 text-slate-600">{event.description}</p><div className="grid gap-4 md:grid-cols-2"><StatCard label="Thời gian" value={toDate(event.start_time)} hint={toTimeRange(event.start_time,event.end_time)}/><StatCard label="Địa điểm" value={event.location} hint={event.organizer}/></div></div></article><aside className="h-fit rounded-[2rem] border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-2xl font-bold">Đăng ký tham gia</h2><p className="mt-3 text-sm leading-6 text-slate-600">Nhập thông tin sinh viên. Hệ thống sẽ tạo mã QR thật từ Supabase để điểm danh.</p><div className="my-5 rounded-3xl bg-slate-50 p-5 text-sm text-slate-600"><p>👥 {event.registered_count}/{event.capacity} đã đăng ký</p><p className="mt-2">🏫 {event.faculty}</p></div><form action={registerStudentAction} className="grid gap-3"><input type="hidden" name="event_id" value={event.id}/><Field name="full_name" label="Họ tên" required/><Field name="email" label="Email" type="email" required/><Field name="student_code" label="Mã sinh viên" required/><SelectField name="faculty" label="Khoa" options={faculties.map(f=>f.name)}/><button className="rounded-2xl bg-blue-700 px-5 py-3 text-center font-bold text-white">Đăng ký và nhận QR</button></form><Link href="/events" className="mt-3 block rounded-2xl border border-slate-200 px-5 py-3 text-center font-bold text-slate-700">Quay lại danh sách</Link></aside></div></PageShell>}

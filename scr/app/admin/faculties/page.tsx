import { AdminLayout } from "@/components/admin-layout";import { DataTable } from "@/components/ui";import { getEvents, getFaculties } from "@/lib/supabase-rest";export default async function Faculties(){const [faculties,events]=await Promise.all([getFaculties(),getEvents()]);return <AdminLayout title="Quản lý khoa"><DataTable headers={["Tên khoa","Số sự kiện"]} rows={faculties.map(f=>[f.name,events.filter(e=>e.faculty===f.name).length])}/></AdminLayout>}
import { AdminLayout } from "@/components/admin-layout";import { DataTable } from "@/components/ui";import { getCategories, getEvents } from "@/lib/supabase-rest";export default async function Categories(){const [categories,events]=await Promise.all([getCategories(),getEvents()]);return <AdminLayout title="Quản lý loại sự kiện"><DataTable headers={["Loại sự kiện","Số sự kiện"]} rows={categories.map(c=>[c.name,events.filter(e=>e.category===c.name).length])}/></AdminLayout>}
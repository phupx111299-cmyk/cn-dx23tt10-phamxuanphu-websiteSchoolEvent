import { PageShell, SectionTitle } from "@/components/ui";import { getPublishedEvents } from "@/lib/supabase-rest";export default async function CalendarPage(){const events=await getPublishedEvents();return <PageShell><SectionTitle eyebrow="Lịch" title="Lịch sự kiện tháng 06/2026" description="Giao diện lịch giúp sinh viên nhìn nhanh các sự kiện trong tuần hoặc tháng."/><div className="grid gap-4 md:grid-cols-7">{Array.from({length:30},(_,i)=>i+1).map((day)=>{const dayEvents=events.filter((event)=>new Date(event.start_time).toLocaleDateString('vi-VN',{timeZone:'Asia/Ho_Chi_Minh'}).startsWith(String(day).padStart(2,'0')));return <div key={day} className="min-h-32 rounded-3xl border border-slate-200 bg-white p-4"><p className="font-bold text-slate-950">{day}</p><div className="mt-3 grid gap-2">{dayEvents.map((event)=><div key={event.id} className="rounded-2xl bg-blue-50 p-2 text-xs font-semibold text-blue-800">{event.title}</div>)}</div></div>})}</div></PageShell>}

import { PageShell, SectionTitle } from "@/components/ui";export default async function QRPage({searchParams}:{searchParams:Promise<{token?:string}>}){const {token}=await searchParams;const value=token||"Đăng ký sự kiện để nhận mã QR";return <PageShell><SectionTitle eyebrow="Điểm danh" title="Mã QR tham dự" description="Đưa mã này cho ban tổ chức quét hoặc nhập vào trang điểm danh."/><div className="mx-auto max-w-md rounded-[2rem] border border-slate-200 bg-white p-8 text-center shadow-sm"><div className="mx-auto grid size-64 grid-cols-8 gap-1 rounded-3xl bg-white p-4 ring-1 ring-slate-200">{Array.from({length:64},(_,i)=><span key={i} className={((value.charCodeAt(i%value.length)||i)+i)%3===0?"rounded-sm bg-slate-950":"rounded-sm bg-slate-100"}/>)}</div><h2 className="mt-6 text-xl font-bold">QR token</h2><p className="mt-2 break-all rounded-2xl bg-slate-50 p-4 text-sm text-slate-600">{value}</p></div></PageShell>}
import { DataTable, PageShell, SectionTitle } from "@/components/ui";import { getNotifications } from "@/lib/supabase-rest";export default async function StudentNotifications(){const notifications=await getNotifications();return <PageShell><SectionTitle eyebrow="Sinh viên" title="Thông báo"/><DataTable headers={["Tiêu đề","Sự kiện","Kênh","Ngày gửi"]} rows={notifications.map(n=>[n.title,n.event_title||"Toàn trường",n.channel,n.sent_at?new Date(n.sent_at).toLocaleString('vi-VN'):'Chưa gửi'])}/></PageShell>}